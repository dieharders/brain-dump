// export { auth as middleware } from './auth'

// export const config = {
//   // This applies next-auth to all non /api routes before they are rendered.
//   matcher: ['/((?!api|_next/static|_next/image|favicon.ico).*)'],
// }

const noop = () => {}
export default noop
